import config from './rollup.config.aio'
import uglify from 'rollup-plugin-uglify-es';

config.output.file = 'dist/index.aio.min.js';
config.output.sourcemap = true;
config.plugins.push( uglify() );

export default config;
